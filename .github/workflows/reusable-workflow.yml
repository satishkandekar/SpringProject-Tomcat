name: CI/CD Pipeline

on:
  push:
    branches: [main, master, feature/**]
  pull_request:
    branches: [main, master]

jobs:
  build-container:
    uses: ./.github/workflows/docker-builde.yml
    with:
      image-name: 'satishkandekar/spring-boot-app'
      java-version: '8'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  code-scan:
    needs: build-container
    uses: ./.github/workflows/sonarcloud.yml
    with:
      project-key: 'satishkandekar_spring-boot-hello-world'
      organization: 'satishkandekar'
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}